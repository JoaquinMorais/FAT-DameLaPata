Pets_methods:
    GetPets
    GetSinglePet

query_methods:
    GetColors(GetAllColors) -- done
    getCharacteristics(GetAllCharacteristics) -- done
    
    #added#
    GetAllSizes
    GetAllCategories

session_methods:
    SendLogin -- done
    SendRegister -- done
    GetProfile --  done
    LogOut -- done

adopter_methods:
    CreateRequest -- done
    GetPreference

shelter_methods:
    PutDogs -- done the same

-- methods --

  GetAllColors().then(response => {
    const data = response.data.response;
    console.log(data);
    setIsLoading(false);
  })
  .catch(error => {
    console.error(error);
    setIsLoading(false);
  });
